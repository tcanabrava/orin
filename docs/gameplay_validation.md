# Gameplay Loop Validation

A structured, repeatable check that the main gameplay loop still works across all
supported modes — **Play 2D**, **Play 3D**, and **Jam Session** — through startup,
pause, resume, and exit. Run this when changing anything in `harmonicon-gameplay`,
`harmonicon-menu`, the audio pipeline, or song/asset formats.

Each item lists its **expected outcome** and whether it's **automated** (a unit
test guards it) or **manual** (needs a run, because it depends on rendering,
audio output, or loaded assets that can't be asserted headlessly).

## How to run

```bash
cargo test          # automated coverage below
cargo run           # manual checks below (needs a mic, audio out, a display)
```

Navigation to gameplay: **Play → Play Song → (2D | 3D) → artist → song**, or
**Play → Jam Session → Pick a Song → artist → song**. In-game keys: **Esc**
pause/resume, **M** metronome mute, **V** cycle spectrogram.

## Automated coverage (`cargo test`)

| Behavior | Test |
|---|---|
| Menu pages open/close in the correct order | `menu::tests::navigation_graph_opens_and_closes_to_the_right_pages`, `…::changing_page_exits_the_old_before_entering_the_new` |
| Esc pauses then resumes (flag + overlay) | `gameplay::pause_menu::tests::escape_pauses_then_resumes` |
| Resume button stays in gameplay | `…::resume_button_unpauses_without_changing_state` |
| Restart reloads the song | `…::restart_button_reloads_the_song` |
| **Quit Song returns to the song list** | `…::quit_song_returns_to_the_song_list` |
| **Play 3D restores the 2D camera on exit** | `gameplay::gameplay_3d::tests::leaving_3d_restores_the_2d_camera` |
| Scene teardown despawns only `GameplayRoot` | `gameplay::tests::cleanup_despawns_only_gameplay_entities` |
| Scoring / sustain / techniques / pitch logic | `gameplay::scoring::tests`, `gameplay::tests::*`, `audio_system::*` |
| Clock re-anchors to the audio sink, clamped so it can't jump | `gameplay::tests::advance_clock_*` |
| Detector range derives from the harmonica layout (low-keyed harps not cut off) | `song::harmonica::tests::frequency_range_*`, `audio_system::pitch_detect::tests::pitch_range_from_freqs_*` |
| Vibrato/wah bonus requires the measured rate to match the chart's `oscillation_hz` | `gameplay::scoring::tests::measured_oscillation_hz_*`, `…::oscillation_matches_rate_*`, `gameplay::tests::technique_confirmed_rejects_*_at_the_wrong_rate` |
| Overlapping same-pitch notes credit the one actually due, not query order | `gameplay::tests::score_notes_credits_the_closest_offset_when_two_same_pitch_notes_overlap` |
| End-to-end: a synthetic pitch stream drives a mini chart through hit/good/miss and the score/combo/stats update accordingly | `gameplay::tests::end_to_end_synthetic_song_drives_score_combo_and_stats` |
| Loop boundary rewinds the clock and resets only the notes inside the loop range | `gameplay::tests::loop_boundary_rewinds_the_clock_and_resets_notes_in_range`, `…::loop_boundary_is_a_no_op_before_end_time_or_when_inactive` |
| Windowed note-visual spawning: a note's window opens/closes at the right time, already-spawned notes aren't respawned, far-out notes are excluded | `gameplay::tests::notes_needing_spawn_*` |
| Adaptive difficulty: phrase grouping, unlock-fraction curve, per-note unlock/section tagging, learned-fraction bump on a clean clear | `gameplay::adaptive_difficulty::tests::*` |
| Pause-menu phrase selector/adaptive-difficulty labels and stepping | `gameplay::pause_menu::tests::next_phrase_index_*`, `…::prev_phrase_index_*`, `…::phrase_selector_text_*`, `…::adaptive_difficulty_label_*`, `…::adjust_learned_*` |
| Progress-bar phrase-section rectangle geometry/color | `gameplay::song_progress_overlay::tests::phrase_rect_geometry_*`, `…::phrase_fill_color_*` |
| Progress-bar note-marker rectangle geometry — horizontal (proportional to duration, floored/clamped) and vertical hole-lane placement (highest hole at top) — and the no-background-music duration fallback | `gameplay::song_progress_overlay::tests::note_marker_geometry_*`, `…::note_lane_geometry_*`/`highest_hole_is_*`/`lowest_hole_is_*`/`middle_hole_lands_*`/`lanes_are_narrower_*`, `…::effective_duration_*` |

## Manual checks

### All modes — entering `Playing`
- [ ] **Audio starts.** After the 3-2-1 countdown, the backing track plays. *(manual: audio output)*
- [ ] **A song missing optional assets still loads and plays, without hanging on `SongLoading` or logging an asset error.** Play "Example Song 3" (`assets/songs/Example Artist/Example Song 3/` — ships only `song/chart.harpchart`, deliberately, no `background.png`/`elements.png`/`song/*.ogg`/`2d/`/`3d/`): the countdown should still run, the chart should still play and score normally against a generated gradient background, with the theme's default note art, and **silently — no backing track, no error dialog, no stuck loading screen.** Load "Example Song" too (its chart is named `example_chart.harpchart`, not `chart.harpchart`) and confirm it's found and loads the same as any other song — song discovery matches on the `.harpchart` extension, not a fixed filename. *(manual: rendering + audio; the file-discovery/fallback logic is covered by `tests/asset_layout.rs::song_assets_are_complete` and `song::loader::tests::generate_background_image_*`, but the actual no-hang, no-backing-track behavior needs a live run — this is exactly the class of bug a passing `cargo test` can't see, since the old bug was a dependency that never resolved, not a panic)*
- [ ] The HUD score/combo reads `0` and updates as you hit notes. *(the
  underlying score/combo/stats math is now covered end to end by
  `gameplay::tests::end_to_end_synthetic_song_drives_score_combo_and_stats`;
  this check is now just about the HUD actually *displaying* those numbers —
  manual: rendering)*
- [ ] No errors/panics in the console while the gameplay chain runs. *(manual)*
- [ ] **Long-song sync**: play a 3+ minute song end to end; the hit line still matches the beat at the end, with no accumulating drift. *(manual: audio + timing; correction math is unit-tested but real decoder/frame-hitch drift isn't)*
- [ ] **Low-keyed harp detection**: load (or author) a chart with a Low-F/Low-D harmonica and confirm hole-1 blow/draw register — the detector range now derives from the chart's layout instead of a fixed 200 Hz floor. *(manual: needs a real low-keyed harp and mic)*
- [ ] **Looping doesn't speed up the music.** With `chart.loop.repeat = true` or the pause-menu A–B loop, let the loop wrap a few times: the backing track should audibly stay in sync with the notes/metronome, not creep faster each pass. *(manual: needs a live `AudioSink`; the clock-rewind and note-reset logic is unit-tested — see the table above — but `AudioSink::try_seek` itself needs real audio output)*
- [ ] **Looped notes replay correctly on screen.** On a loop wrap, notes inside the loop range should reappear and be hittable again exactly as on the first pass (not stuck showing a stale hit/miss tint, not missing, not duplicated). This took two fixes: (1) `update_note_visuals`/`update_note_visuals_3d` only ever tinted a note gold (hit) or red (missed) and otherwise left its current color alone — so a note hit (or missed) right before the wrap, whose visual entity hadn't yet despawned off the bottom of the screen, kept showing that stale tint after `handle_loop_boundary` cleared its `hit`/`missed` flags, instead of going back to its normal blow/draw colour; fixed by always resetting to the base colour in the "neither" case (`gameplay_2d::note_tint`, `gameplay_3d::note_tint_3d`). (2) `handle_loop_boundary` only reset notes strictly inside `start_time..end_time` — but `notes_needing_spawn` previews notes up to `LOOKAHEAD` (3s) ahead of the clock, so a note just *past* `end_time` (reachable as a preview once the clock rewinds close to it, even though the loop itself never actually gets there) kept showing whatever hit/miss tint an earlier, unrelated pass left it in — most visible with a loop range shorter than `LOOKAHEAD`, or one set behind where the song had already played to. Fixed by extending the reset range to `end_time + LOOKAHEAD` (`loop_reset_range`). *(manual: rendering; both fixes are unit-tested — `gameplay_2d::tests::note_tint_restores_*`, `gameplay_3d::tests::note_tint_3d_restores_*`, `gameplay::tests::loop_reset_range_*` — but note visuals are spawned/despawned dynamically rather than kept alive across the whole song, so seeing it actually happen live still needs a real loop wrap, ideally with a short loop range)*
- [ ] **A song whose audio ends before `SongEnd` doesn't stutter-loop near the end.** With a chart whose `music.ogg` is trimmed tight to (or shorter than) the last note (no padding beyond `SONG_END_TAIL` = 2.5s), let it play to completion: once the audio actually finishes, the clock/note-highway/progress-bar playhead should keep moving forward smoothly (frame-timer driven) until the results screen appears — not repeatedly snap backward roughly every half second. (Root cause: a finished `AudioSink`'s `position()` freezes rather than continuing to advance; anchoring the clock to a frozen position drifted the clock past `SNAP_THRESHOLD_SECS` and snapped it back, over and over. Fixed by no longer anchoring once the sink is empty — `gameplay::tick_clock` / `should_anchor_to_sink`.) *(manual: needs a live `AudioSink` with a short/tightly-trimmed track; the gating logic is unit-tested — `gameplay::tests::does_not_anchor_once_the_sink_is_empty` and friends — but the audible/visual stutter itself needs a real run)*
- [ ] **Song-progress bar shows the real waveform.** On entering Playing, the top progress bar should show the song's actual amplitude contour (not a flat bar) immediately — it's pre-analyzed at asset-load time, not decoded during setup. A thin red playhead line sweeps left to right in sync with the music instead of a growing fill. Confirm it degrades gracefully (flat/empty bar, no panic) if `music.ogg` were ever undecodable. *(manual: rendering; the bucketing/downmix math is unit-tested — `audio_system::waveform::tests::*` — but the visual shape is only confirmed by looking at it)*
- [ ] **Note markers under the waveform line up with the notes themselves, one lane per hole.** The strip below the waveform should span the harmonica's full hole range — the highest hole's lane at the top, the lowest (hole 1) at the bottom, evenly divided (10 lanes on a diatonic, 12 on a chromatic) — with one colored rectangle per chart note in its own hole's lane: blue for blow, orange for draw (the same hues the Song Editor's scrollbar minimap uses), width proportional to the note's own duration (a held note visibly wider than a quick one). A dense phrase should look denser, a chord should show multiple simultaneous lanes lit at once, and the markers a note highway hit lines up with visually should be the ones the playhead is crossing at that instant, in the row matching that note's hole. *(manual: rendering; the timescale math is the same `AudioDuration`-based fraction already unit-tested for the playhead/loop marker, and the marker's own width/floor/clamp and hole-lane math are unit-tested (`note_marker_geometry_*`, `note_lane_geometry_*`), but marker placement itself is only confirmed by looking at it)*
- [ ] **Progress bar still shows phrase rects/note markers on a song with no background music.** Pick (or author) a song whose chart has no `song/music.ogg`/`.wav`. The progress bar should still appear with the note-marker strip and (if the song has phrase sections) the phrase strip populated — laid out against the chart's own note/phrase extent rather than reading empty just because there's no audio to measure. Only the waveform row itself should stay blank, since there's genuinely no waveform without decoded audio; the playhead should still sweep across the bar as the song plays. *(manual: rendering; the fallback math is unit-tested — `song_progress_overlay::tests::effective_duration_*` — but only a live run against a music-less chart confirms the bar actually populates)*
- [ ] **Adaptive Difficulty is off by default and lives in Options, globally.** On a fresh profile/settings file, Options should show "Adaptive Difficulty: off"; playing any song should show every note regardless of prior progress. Turn it on in Options: playing *any* song should now gate notes — confirming it's one setting shared by every song, not something picked per song. *(manual: rendering; the default is unit-tested — `settings::tests::adaptive_difficulty_is_off_by_default` — but only a live run confirms Options actually gates real gameplay)*
- [ ] **Adaptive difficulty starts sparse and grows with clean clears (Play 2D/3D only, with the Options toggle on).** On a song with no prior profile record, only a sparse subset of each phrase's notes should appear (starting at the phrase's first note(s), not scattered) — the rest of that phrase is simply absent from the highway, not shown-but-unhittable. Clear a phrase cleanly (every note that *did* appear, hit with no misses) through to the results screen, then Restart: more of that phrase's notes should now appear, starting from the beginning of the phrase. Toggle "Adaptive Difficulty" off in the pause menu, Restart, and confirm every note in every phrase now appears regardless of prior progress — and that the Options page now also shows it off, since the pause-menu toggle changes the same global setting. *(manual: rendering + a full playthrough; the unlock-fraction curve, prefix selection, and clean-clear bump are unit-tested — see the table above — but only a live run confirms notes actually (dis)appear from the highway)*
- [ ] **Progress-bar phrase overlay reflects learned progress.** The song-progress bar should show one translucent rectangle per musical phrase *overlaid on the note-lanes strip itself* (not a separate row below it), colored from dim gray (unlearned) toward green (fully learned), with the note markers still clearly visible/legible on top of the tint rather than obscured by it. *(manual: rendering; the geometry/color math is unit-tested)*
- [ ] **Manual phrase override (pause menu + progress-bar overlay, Play 2D/3D only).** Esc into the pause menu, then click a phrase's rectangle overlaid on the song-progress bar's note-lanes strip: its border should turn gold (the only on-bar sign of which section is selected), the pause menu's "Section: <name> — Learned: NN%" readout should update to match, and the "Learned" slider below it should jump to that phrase's current value. Drag the slider: confirm the corresponding progress-bar rectangle re-tints immediately, and that Resume shows more (or fewer) notes in that phrase on the highway right away — no Restart needed. Clicking the waveform band above (outside any phrase rect's coverage) should start an A–B loop drag instead, not change the phrase selection. Notes already hit/missed before the change should keep their state (not reset to unhit) even though the note list was rebuilt underneath them. Toggling "Adaptive Difficulty" off/on mid-song should likewise unlock/relock every note live. *(manual: rendering; the rebuild-and-carry-over logic is unit-tested — `adaptive_difficulty::tests::carry_over_*`, `…::first_unresolved_index_*` — but only a live run confirms notes actually (dis)appear from the highway without a restart and without losing progress, and that a phrase-rect click doesn't also start a loop drag)*
- [ ] **A–B loop controls (drag on the progress bar's waveform band, while paused).** Esc into the pause menu: the song-progress bar (waveform + note-lanes strip) should stay visible and clickable *through* the pause dimming, not get covered by it. Click-and-drag in the waveform band (the note-lanes strip below it is now covered edge-to-edge by the phrase overlay wherever a phrase section exists, which is the section picker instead — see the phrase-override item above): a yellow semi-transparent rectangle should sweep out live, covering the bar's full height, following the drag in either direction. Release the mouse — the readout should switch from "Loop: off" straight to "Loop: Ns–Ns" (no intermediate state), and Resume should have the song now loop that exact section (offset + duration from where you dragged, no bar-snapping). A drag with ~zero movement (a near-click) should leave the loop off rather than activating a zero-length range. Click "Clear Loop" and confirm it stops looping and the marker disappears immediately, live, even while a drag preview would otherwise be showing. *(manual: rendering + audio; the cursor→time mapping, drag-direction normalization, and request→`LoopConfig` application are unit-tested — `song_progress_overlay::tests::cursor_to_time_*`, `…::drag_marker_geometry_*`, `…::apply_requested_loop_range_*` — but the actual sink seek needs real audio output)*
- [ ] **Wait-for-Note mode freezes and resumes cleanly (Play 2D/3D only — Jam Session has no chart notes to freeze on).** Esc into the pause menu, click "⏸ Wait for Note" to turn it on, Resume: the note highway should stop scrolling and the music should stop the instant an unhit note reaches the hit line, both holding steady (not stuttering, not slowly drifting) until you actually play the correct pitch — at which point it scores and everything resumes at normal speed immediately. Toggle it off mid-song and confirm notes go back to missing normally if you don't hit them in time. *(manual: needs a live `AudioSink`; the freeze condition itself is unit-tested — `gameplay::tests::first_due_unresolved_note_*` — but only a live run shows the clock/audio actually holding steady rather than stuttering)*
- [ ] **A "Play Hole N ↑/↓" prompt appears while frozen** (`wait_freeze_overlay`), naming the actual due note, and disappears the instant it's hit or the mode is toggled off. Without it a frozen clock looks exactly like a hang. *(manual: rendering)*
- [ ] **Microphone input stays clean while frozen.** With Wait for Note on, let it hold on a note for a few seconds while talking/playing near the mic: pitch detection should behave exactly as it does unfrozen, with no dropouts, crackle, or delayed response. (`tick_clock` used to call `AudioSink::pause`/`play` every frame while frozen instead of once on the transition, which was suspected of disturbing a shared audio graph/server and affecting the — otherwise fully separate — mic capture pipeline; fixed by only touching the sink on the actual freeze/unfreeze edge.) *(manual: audio input; can't be asserted headlessly)*
- [ ] **Practice speed slows the highway and metronome without stuttering, and mutes rather than pitch-shifting.** Esc into the pause menu, drag the "🐢" Speed slider down from 100% toward 50%: the note highway and metronome should visibly/audibly slow to match the label, and the music should go silent below 100% (no pitch-shifted/chipmunk audio) rather than stutter or race ahead. Drag back to 100% mid-song and confirm the music resumes in sync with the highway (not skipped ahead or lagging) and the note highway doesn't jump or stutter across the transition. Also check the interaction with Wait for Note: turn both on, let it freeze on a note, then drag Speed — the freeze should hold exactly as before (frozen takes priority over slow-but-playing). *(manual: needs a live `AudioSink`; the label formatting is unit-tested — `pause_menu::tests::practice_speed_label_*` — but the slider interaction and the audio pause/reseek/resume behavior need a live run)*
- [ ] **Tab ribbon shows the current phrase's notation.** Below the phrase/groove banner in the sidebar, a line like `-4' +5 -4` should appear once the song reaches a chart phrase, listing every note in that phrase (not just ones already played) as `+N`/`-N` with bend apostrophes, an `o` for overblow/overdraw, or a `*` for a chromatic slide. It should update the instant a new phrase starts, and read empty before the first phrase. *(manual: rendering; the tab-string derivation and phrase-windowing are unit-tested — `phrase_overlay::tests::tab_label_*`, `…::phrase_tab_sequence_*` — but only a live run against a real chart confirms it reads correctly against the falling notes)*
- [ ] **"Note labels" option (Options page) swaps the arrow for the hole number, and persists.** Turn it on, start a song: every falling note should show its hole number instead of ↑/↓ — centered on the head in 2D, in a small dark pill just above-left of the note in 3D. Turn it off and confirm arrows come back. Restart the app and confirm the choice survived (`settings.json`). *(manual: rendering; the label text/toggle logic is unit-tested — `menu::options::tests::note_numbers_label_*` — but only a live run shows the actual head/overlay placement)*
- [ ] **A chart's declared `song.feel` sets the metronome's straight/shuffle click on song start.** Author (or edit by hand) a chart with `"feel": "straight"` and start it with the metronome currently set to Shuffle from a previous song: the click should switch to straight subdivision (plain quarters, no swung "and") the moment the song starts, and the feel-toggle pill's label should read "feel: straight" to match. A chart with no `feel` field (the common case) should leave whatever feel was last selected untouched. *(manual: audio; the chart→`MetronomeFeel` mapping is unit-tested — `metronome_overlay::tests::feel_from_chart_*` — but only a live run confirms the click itself audibly changes)*

### Play 2D
- [ ] The note **highway shows falling notes** in the ten lanes, with the comet head + animated tail. *(manual: rendering)*
- [ ] The **HUD** (song info, 12-bar grid, metronome, technique legend, score) is visible on the right. *(manual)*
- [ ] Notes recolour gold on hit / red on miss; long notes reward holding the pitch. *(manual; sustain logic is unit-tested)*
- [ ] **Notes appear and disappear cleanly** — no pop-in right at the top of the highway, no note lingering after it's scrolled off, no duplicate/ghost note. *(manual: rendering; note visuals are now spawned in a `LOOKAHEAD` window rather than all up front — the windowing math is unit-tested (`notes_needing_spawn_*`), but only a live run shows whether it's visually seamless)*

### Play 3D
- [ ] The 3D scene initializes: lane floor, hit zone, the harmonica model, and comet notes travelling down the lane. *(manual: rendering + GLB asset)*
- [ ] **Leaving 3D restores the 2D menu camera** (menu renders normally afterward — order/clear reset). *(automated; verify visually too)*
- [ ] Exiting despawns the 3D scene (no leftover meshes/cameras on the next song). *(teardown automated; verify visually)*
- [ ] **Notes appear and disappear cleanly**, same windowed-spawning check as 2D above. *(manual: rendering)*
- [ ] **Hole-number labels (when the option is on) track their note correctly.** Each label should stay glued to its note's on-screen position as it travels down the lane — no lag, no drift, no jitter — and should disappear the instant its note scrolls past behind the camera (not linger frozen on screen) or the moment the note is recycled. Check a chord/split with several simultaneous notes: each gets its own correctly-numbered label, not overlapping into one unreadable blob. Also press the UI-zoom keys (Arrow Up/Down, `dialogs::ui_scale`) mid-song and confirm labels stay glued to their notes at every zoom level — they were landing far off to the side at anything other than the default zoom until `note_label_position` accounted for `UiScale` (previously divided by the window's own scale factor, which `Camera::world_to_viewport` had already divided out; `UiScale` is a separate multiplier bevy_ui applies on top and was never divided out at all). *(manual: rendering; the label-position math is unit-tested — `gameplay_3d::tests::note_label_position_*` — but the world-to-screen projection itself has no headless test, since it needs a real camera/window)*

### Jam Session
- [ ] Starts and runs **without falling notes** — the 12-bar chart + metronome drive it; no chart-note highway is required. *(manual)*
- [ ] Does not transition to the Results screen on its own (it has no finite song end). *(manual)*
- [ ] **Toggling Loop keeps the music playing.** Click "Loop" on and off a few times, including after the jam has run past one full play-through of the backing track: the music should keep playing (looping when on, stopping only naturally when off), never go silent. *(manual: needs a live `AudioSink`. Four earlier attempts all despawned the live `MusicPlayer` entity and respawned it mid-track in reaction to the toggle, and all four went silent for various reasons (see TODO.md) — resuming from the gameplay clock, resuming from the sink's own `position()`, an explicit `.with_start_position(Duration::ZERO)` restart, and even dropping `.with_start_position()` entirely. The actual fix is `restart_finished_jam_music`: Jam Session's music always spawns as `PlaybackSettings::DESPAWN` (self-despawns on completion), the toggle itself does nothing, and a new entity is only ever spawned after the previous one is already gone and Loop is on at that moment — never touching a live sink at all. Treat any future logic that reacts to the toggle by touching the *current* sink as regression-prone until proven otherwise)*
- [ ] **"Generate Jam" (inside the Jam Session submenu, alongside "Pick a Song") skips picking a song entirely.** Cycle Key, Tempo, and Progression with the ◂/▸ buttons, click Start Jam: should count down straight into an ordinary Jam Session — 12-bar grid, hole map, spectrogram — with an audible synthesized walking bass line (not a harmonica timbre) following the *picked* progression's chord roots in the picked key, looping metronome-locked to the picked tempo. **Restart** from the pause menu should reset the bass to the top of the loop without hanging on a loading screen. **Quit Song** should return to the **Generate Jam page** (key/tempo/progression remembered), not the song list. *(manual: needs audio out; the bass-generation math, chart assembly, and the manifest's asset presence are unit-tested — `jam_backing::tests::*`; the state-routing (`GeneratedJamSession`, skipping `SongLoading`) is unit-tested indirectly via `gameplay::pause_menu::tests::restart_can_target_playing_directly_for_a_generated_jam` but the actual no-hang behavior needs a live run)*
- [ ] **Selectable progressions match audio, grid, and hole map.** Pick "Quick Change" or "Minor Blues" on the Generate Jam page (or note that the real-song "Pick a Song" button always plays Standard, resetting any previous generated-jam pick): the 12-bar grid's chord-colour bands, the hole-map chord-tone highlighting, and the audible bass line should all agree with each other and with the picked progression's actual chord sequence — no lane says one chord while the bass plays another. *(manual: needs audio out; the chord-sequence math (`progression_bars`), grid coloring (`twelve_bar_blues_overlay::bar_bg`), and hole-map chord tones (`jam_session::build_hole_guide`/`chord_tone_classes`) are all unit-tested against each progression variant, but only a live run confirms the three actually agree on screen/ear at once)*

### Bending Trainer
- [ ] **Two-column layout, same split as Jam Session.** The left column has the title, key control, Detect-algorithm picker, ear-training target/Listen, tuner readout, drill toggle, tempo control, and a Back button; the right column is only the bend diagram plus its technique hint/drill explanation text. Neither column's content should overflow into the other. *(manual: rendering)*
- [ ] **Back button and Esc both leave to the Play menu, not Main.** Click **Back** (bottom of the left column): should return straight to **Play** (this screen has no pause menu, unlike a song/Jam Session). Re-enter and press **Esc** instead: same result. Drill progress should be saved either way (re-enter and confirm hit rates from before are still there). *(manual: this is the only screen with no pause menu and no song, so a visible Back button matters for keyboard-less/mobile input; `ReturnToPlay` is unit-tested indirectly via `menu::tests::*` for the routing mechanism, but the button's presence and the drill-save-on-exit behaviour need a live run)*
- [ ] **Detect-algorithm dropdown dismisses from a click anywhere, including the right column.** Open the "Detect" combobox (left column), then click somewhere in the right column (e.g. on the bend diagram): the dropdown should close, same as clicking anywhere else on the page. This exercises `combobox::spawn_combobox`'s split trigger/backdrop parents (the trigger lives in the left column, but the backdrop is still sized to the whole page) — a regression here would mean the backdrop only covers the left half. *(manual: rendering/picking, no automated coverage — this is precisely the class of bug headless tests can't see)*

### Pause / resume / exit (all modes)
- [ ] **Esc pauses**: gameplay freezes, the music pauses, the PAUSED overlay appears. *(automated for flag/overlay; audio manual)*
- [ ] **Esc again resumes**: overlay hides, music resumes, gameplay continues. *(automated for flag/overlay)*
- [ ] **The on-screen ⏸ button, bottom-right of Play 2D/3D and Jam Session, pauses exactly like Esc** (same overlay, same music pause), and is not clickable/visible-through once the pause overlay is up (the overlay is a full-screen backdrop on top of it). *(manual: rendering/picking; the shared toggle logic itself is exercised indirectly by the existing Esc pause/resume tests since both call the same `toggle_pause` helper — `gameplay::pause_menu::toggle_pause`)*
- [ ] **Resume** button behaves like Esc-resume. *(automated)*
- [ ] **Restart** reloads the same song from the countdown. *(automated for the state change)*
- [ ] **Quit Song** returns to the **song list** (not the main menu). *(automated)*

### Lessons
- [ ] **Main menu shows "Lessons" directly below "Play"** (flex-flow order — themes no longer place buttons by pixel coordinate), and it opens the lesson list: a **tab bar of units** (Unit 1 · Blowing / Unit 2 · Counting / Unit 3 · Blues Vocabulary) over a **vertical scrollbox** of the selected unit's lessons. Clicking a tab swaps the list without leaving the page (green = active tab; clicking the active tab does nothing); the chosen tab is remembered when returning from a lesson. The scrollbox wheel-scrolls once a unit has more lessons than fit. On a fresh profile, Unit 1 shows "Playing a Single Note" clickable and "Hand Shape and the Wah" dimmed with a 🔒 (its prerequisite is single-note); Unit 2 shows "Reading the 12-Bar Blues Grid" clickable. *(manual: rendering + interaction; the unlock logic, unit grouping, and tab retinting are unit-tested — `lessons::tests::is_unlocked_*`, `…::grouping_preserves_lesson_and_unit_order`, `dialogs::tab_bar::tests::*`)*
- [ ] **Reader page** shows the localized body text (wrapped, not spanning the full window), a gold "Goal: ..." line for chart lessons, and Start Lesson / Back. The 12-bar lesson shows "Mark as Done" instead of Start; clicking it returns to the list with a ✓ on that lesson. Switch locale (pt-BR/es-ES) and confirm the body text follows. *(manual: rendering + localization)*
- [ ] **Start Lesson launches the drill through normal 2D gameplay**: countdown, falling notes, metronome — with **every note present** (adaptive difficulty must not gate lesson notes, even after a prior partial run). The single-note drill is silent backing + metronome by design. *(manual; the adaptive-off-for-lessons gate is code-reviewed but needs a live run to confirm notes appear)*
- [ ] **Results screen judges the lesson**: a green "LESSON PASSED" (≥60% clean-attack rate on the single-note drill — hitting the note on time isn't enough if a second, unintended pitch sounds alongside it, e.g. a breathy multi-hole leak) or orange goal-not-reached banner appears under the grade; no "Best score"/"NEW BEST" song row for lesson runs. A "Clean attack" row also appears in the by-technique breakdown on *any* song's results screen, not just this lesson. Retry keeps judging as a lesson. Continue (or Esc, or pause→Quit Song) returns to the **lesson list**, not the song list. *(manual: rendering + flow; the pass judgment is unit-tested — `lessons::tests::lesson_passed_*` and `accuracy_criterion_*`/`technique_criterion_*`; the clean-attack primitive is unit-tested — `scoring::tests::*clean_attack*`)*
- [ ] **Passing single-note unlocks hand-wah** on the next visit to the list (🔒 gone, button clickable), and the passed lesson shows ✓ but stays replayable. The pass survives a game restart (`profile.json`). *(manual; persistence logic unit-tested — `profile::tests::*lesson*`)*
- [ ] **Hand-wah drill passes on the wah**, not just the pitch: holding clean steady notes without cupping should fail the ≥50% wah-technique goal; opening/closing the cup ~2×/second on the held notes should pass it. *(manual: needs a real mic + hands)*
- [ ] **Multiple-notes drill passes on simultaneity, not sequence**: playing the 1-2-3/2-3-4/4-5-6 chord's holes one at a time (even correctly, even fast) should fail the ≥50% overall-accuracy goal — an out-of-sync chord note just reads as an ordinary miss; sounding all holes of a chord together should pass it. Unlocks after single-note. **Octave-split drill** (unlocks after multiple-notes) works the same way on holes 1+4, 2+5, 3+6 — playing only one hole of a pair should never score. *(manual: needs a real mic and two hands/tongue-blocking to actually voice a chord; the simultaneity requirement itself is unit-tested — `scoring::tests::*chord_is_sounding*`, `gameplay::tests::score_notes_*chord*`)*
- [ ] **Tongue-blocking lesson is text-only** (unlocks after multiple-notes): reader page shows "Mark as Done" instead of Start (no chart), and its body explicitly points at the octave-split lesson as the scoreable payoff. **Slides drill** (unlocks after single-note) has two phrases: an adjacent-hole run (4-5-6 blow, twice) and a bent-then-released note (2 draw bent a half step, held, then the same hole played natural) — the bend phrase should only score if you actually bend the pitch down at onset, not just play the natural note early. *(manual: needs a real mic and a harp capable of a 2-draw bend)*
- [ ] **Call-and-response drill plays a demo before each phrase, then waits**: Start Lesson (unlocks after the 12-bar-grid lesson) counts down into normal 2D gameplay; a few seconds before each of the three phrases (one, two, then three notes), you should hear a short synthesized harmonica demo of that phrase's notes play once, finishing with a beat of silence before the notes reach the hit line. The falling note(s) then freeze at the hit line — same "Play Hole N ↑/↓" prompt `WaitForNoteMode` shows — **even though the practice toggle is off** — until you actually play the right pitch; there's no way to miss one by being too slow. Play a wrong pitch: nothing happens, it just keeps waiting (no miss/combo-reset). Results screen judges ≥70% accuracy across all six response notes. *(manual: needs real audio out + mic; the phrase-grouping/tick-mapping/freeze-forcing logic is unit-tested — `gameplay::call_response::tests::*`, `gameplay::tests::wait_freeze_index_*`)*
- [ ] **Using-your-feet drill has a noticeably tighter timing window** than the other beginner drills (unlocks after the 12-bar-grid lesson, sibling of call-response): a steady quarter-note pulse on hole 4 blow/draw. Playing right on a felt beat should pass comfortably; playing correct pitches but noticeably early/late/rushing should miss more than it would on, say, the single-note drill. *(manual: needs a real mic; no new logic beyond the chart's own scoring-window values, already covered by the general `classify_note` tests)*
- [ ] **Improvisation lesson opens an ordinary Jam Session, not a scored chart**: Start Lesson (unlocks after the call-and-response lesson) shows the 12-bar grid, hole map, spectrogram — no falling notes, no score/combo HUD. The pause menu shows a **"Finish Lesson"** button that the normal Play2D/3D pause menu doesn't have. Play only chord/scale notes for a bit (hole map mostly gold/green) then Finish Lesson: should show LESSON PASSED (≥80% adherence). Restart, play mostly wrong notes (hole map mostly amber) then Finish Lesson: should fail. Quitting instead of finishing should **not** record an attempt (matches chart-lesson quit behaviour — only a deliberate finish judges it). Either way it returns to the **lesson list**, not the song list. *(manual: needs a real mic; the adherence math and gate/reset behaviour are unit-tested — `gameplay::jam_session::tests::*improv*`, `lessons::tests::scale_adherence_criterion_*`)*
- [ ] **Lessons wave 2, part 1 — spot-check a few of the 12 new drills** (`docs/lessons_plan.md`): `first-bend`/`deep-bends` should only pass on the *bent* pitch, not the natural note played flat; `vibrato` should fail on a rock-steady held tone and pass once you pulse it around 4-5 Hz; `articulation` should fail if you hold one continuous tone across its repeated eighth notes instead of re-tonguing each one (same fresh-attack mechanic as the shipped `single-note`/`multiple-notes` lessons, just proxied for tonguing); `bar-counting`/`turnaround` should only score the sparse root notes they chart, not anything played during the long rests. *(manual: needs a real mic; each technique's onset/sustain scoring is already covered by `scoring::tests::*` and `song_editor::tests::*`, so this is really about confirming the *content* — hole/timing choices — teaches what its body text claims)*
- [ ] **`train-rolling` actually accelerates** (Start Lesson, unlocks after `train-chug`): the chug should audibly speed up in a handful of discrete steps from ~70 to ~110 BPM over the course of the drill, not stay at a fixed tempo — this is the first bundled chart positioned by `tick` + a tempo map instead of fixed-seconds `time`, so a flat tempo here would mean the tick→seconds path is broken. *(manual: needs audio out; `tick_to_seconds`'s stepped-segment math is unit-tested — `song::chart::tests::*tempo*` — but only a live run confirms a chart actually authored with `tick` fields plays back correctly)*
- [ ] **`train-whistle` needs the chord *and* the wah at once** (unlocks after `train-rolling` + `hand-wah`): during a held whistle chord (holes 4+5 draw), playing both holes steadily without cupping should fail the wah-technique goal even though the chord itself sounds fine; adding the ~2×/second hand-wah pulse should pass it. *(manual: needs a real mic + hands; chord simultaneity and wah-oscillation scoring are each already unit-tested independently, but not previously combined on one note — this exercises that combination)*
- [ ] **`blues-scale` (Unit 3, unlocks after `deep-bends`) should only pass on the bent pitches, not the naturals played flat**: 3 draw and the second 4 draw of the sequence need an actual bend at onset (same mechanic as `first-bend`/`deep-bends`), while the surrounding notes (2 draw, 4 blow, the first 4 draw, 5 draw, 6 blow) score on the plain natural pitch. *(manual: needs a real mic capable of 3- and 4-draw bends; onset bend validation already covered by `scoring::tests::*bend*`)*
- [ ] **`first-licks`/`bent-licks` (Unit 3) play a demo before each of their three licks, then wait**, same call-and-response mechanic as the Unit 2 `call-response` lesson: a short synthesized demo plays, then the falling notes freeze at the hit line until you echo them, with no way to miss by being slow. `bent-licks` should additionally only score its bent notes (3 draw, 4 draw) on the actual bend, not the natural. *(manual: needs real audio out + mic; freeze-forcing logic is unit-tested — `gameplay::call_response::tests::*`, `gameplay::tests::wait_freeze_index_*`)*
- [ ] **`licks-over-changes` (Unit 3, unlocks after `bent-licks` + `bar-counting`) shows three phrase-tagged 4-bar lines** in the phrase overlay as it plays through a full 12-bar chorus — confirm the overlay's section boundaries land where the I/IV/V licks actually change, not just anywhere in the bar. *(manual: needs a real mic; phrase-boundary logic is exercised by the existing `phrase_overlay`/adaptive-difficulty tests, this is really a content-authoring spot-check)*
- [ ] **Chord-tone-adherence, minor-blues, and phrase-discipline lessons (Unit 3: `chord-tone-improv`, `minor-blues-improv`, `question-answer`) route into Jam Session like the improvisation lesson does**: same "Finish Lesson" pause-button flow, but judged on a *different* `ImprovStats` fraction each. `chord-tone-improv` should fail on notes that are merely in-scale (not chord tones), where the plain `improvisation` lesson would have passed them. `question-answer` should fail on notes played during a "rest" window of the fixed 2-bars-on/2-bars-off pattern even if the pitch itself was perfect. *(manual: needs a real mic; the fraction-selection and rest-window logic are unit-tested — `gameplay::pause_menu::tests::jam_fraction_for_*`, `gameplay::jam_session::tests::in_rest_window_*` and `*rest_violation*`)*
- [ ] **`minor-blues-improv`'s `progression: "minor"` field seeds the jam's backing on Start**: starting it should hear/see the minor-blues chord sequence immediately, not Standard — and starting an *ordinary* real-song Jam Session afterward should show Standard again, not a leftover minor pick. *(manual; the parsing/reset logic is unit-tested — `menu::lessons::tests::parse_progression_*`)*

### Guided tutorial tour
- [ ] **"Tutorial" button (Help/About menu) starts an auto-advancing tour.** Click it: the screen should switch itself, with no further clicks, through Main → Play → Select Mode → **a live 2D gameplay demo** → Jam Session Menu → **a live Jam Session demo** → Generate Jam → **the Bending Trainer** → Lessons → **the Song Editor** → Options → Theme → Help/About → back to wherever it was started from, each with a dark overlay panel naming the screen, a one-line explanation, and a "Step N of 13" readout. The underlying screen's own buttons should **not** be clickable through the overlay (clicking one shouldn't navigate anywhere off-tour). *(manual: rendering + timing; the step sequence itself is unit-tested — `menu::tutorial::tests::*`)*
- [ ] **The gameplay/Jam Session demo steps show real content, not a blank/frozen screen**: the 2D demo should show the countdown then falling notes from the bundled Example Song; the Jam Session demo should show its 12-bar grid and hole map live; neither should ever reach a Results screen or record anything to your profile — the tour always cuts away first (they run for ~7s, comfortably inside the ~2.5-minute demo song). *(manual: needs a live run; the demo song's length is asserted long enough to never complete a step's duration — `menu::tutorial::tests::every_step_has_a_positive_duration` combined with `DEMO_SONG_PATH`'s known ~152s runtime, not itself asserted in a test since it'd require loading the actual audio asset)*
- [ ] **Skip Tutorial ends the tour immediately from any step — including a live-gameplay one** — and returns to wherever it was started from (Help/About), overlay gone, screen fully interactive again. Letting all 13 steps play out to the end does the same automatically, without a click. *(manual)*
- [ ] **Escape does nothing while the tour is running, on every step type** — it shouldn't pause the gameplay demo, back out of the Bending Trainer, or leave the Song Editor, any more than it should navigate a plain menu page back a level. After Skip or the tour finishing, Escape/pause behave normally again on every screen. *(manual)*

### Song Editor — Erase/Remove timeline tool
- [ ] **Selecting Erase or Remove highlights that button and makes the ruler above the grid interactive**; clicking it again (or the other tool) deselects/switches — neither is selectable at once, and with neither selected, clicking the ruler does nothing. *(manual: rendering + interaction; the pure range math is unit-tested — `song_editor::tests::erase_range_*`/`remove_range_*`, tool-toggle is exercised by `panel::update_timeline_tool_buttons`)*
- [ ] **Click, hover, click places a split and previews a side**: with a tool active, click a point on the ruler — a vertical marker should appear there; moving the mouse left/right of it should highlight that whole side (song start→split, or split→song end) live; clicking on the highlighted side should open a confirm dialog naming the bar range. Escape before that second click cancels the split (marker disappears, no dialog). *(manual: needs mouse hover; range resolution is unit-tested — `song_editor::tests::split_side_range_*`)*
- [ ] **Click-drag-release picks an explicit span instead**: dragging across the ruler should show a live highlighted region following the drag, then open the same confirm dialog on release naming that exact range. Releasing with no movement (zero-length span) should do nothing. *(manual)*
- [ ] **Confirming Erase deletes the notes in range and leaves every other note exactly where it is** (a visible gap on the grid); confirming Remove deletes them *and* shifts every note after the range earlier by the range's length (no gap, song gets shorter). Choosing No, or dismissing via Escape/clicking outside the dialog, leaves the song completely untouched either way. *(manual; the note-list surgery itself is unit-tested — `song_editor::tests::erase_range_deletes_only_overlapping_notes_and_shifts_nothing`, `remove_range_deletes_overlapping_notes_and_shifts_the_rest_earlier`)*
- [ ] **This is a good pairing with MIDI import**: import a MIDI track that starts partway into the file, then use Remove to delete the leading silence and pull the notes back to beat 1. *(manual, needs a real MIDI file with a late-starting track)*

### Song Editor — Silence track
- [ ] **A labeled strip below the last hole lane shows the gap, in seconds, between consecutive notes**: place two notes with a rest between them — a block should appear in that strip spanning the rest, showing its duration (e.g. "1.5s"); moving either note should update the block live on the next edit. A chord (two notes at the same tick, different holes) or back-to-back notes must **not** show a gap between them, and there's no block before the first note or after the last (leading/trailing silence isn't "between" anything). *(manual: rendering; the gap math itself is unit-tested — `song_editor::tests::silence_gaps_*`)*

### Song Editor — MIDI import
- [ ] **Import MIDI lists every track and lets you switch between them**: pick a `.mid`/`.midi` file with several tracks — a dropdown should appear listing each by name (or "Track N" if unnamed) and note count; picking one drops that track's notes onto the grid at the right pitches/timing, and picking a *different* track replaces them rather than adding to them. *(manual: needs a real MIDI file; the parsing/mapping/quantization logic is unit-tested — `song_editor::midi_import::tests::*`)*
- [ ] **Imported notes land on holes/bends/slides that actually sound the right pitch**: switch to Perform mode and Play the imported track (or use Practice against a real harmonica) — it should sound recognizably like the original MIDI track, with bent notes only where the harp genuinely needs a bend to reach that pitch (diatonic) or the slide button lit for a semitone the natural note can't reach (chromatic). *(manual: needs audio out, ideally a real mic too)*
- [ ] **Saving with a MIDI track selected writes a processed `.mid` and a `song/music.wav` next to the chart**, and the original MIDI file you picked is untouched. Re-opening the Song Editor on the saved chart should show `song/music.wav` already reflected in the Background Music field. *(manual: needs filesystem inspection; the track-removal/backing-mix logic is unit-tested — `song_editor::midi_import::tests::remove_track_bytes_*`/`render_backing_pcm_*`)*
- [ ] **That `song/music.wav` actually plays as the song's backing track in real gameplay** (Play 2D/3D on the saved song, not just the editor's own preview) — confirms the `song/loader.rs` OGG→WAV fallback picks it up. *(manual: needs audio out; the loader fallback and WAV waveform decode are unit-tested — `audio_system::wav::tests::decode_wav_pcm16_*`, `audio_system::waveform::tests::analyze_wav_waveform_*`)*

### Song Editor — Record
- [ ] **Clicking Record (Perform mode) changes its label to "Stop Recording"**, and playing a real harmonica adds notes to the grid live, growing in real time: a note should appear on the grid the instant you start playing it (at minimum length) and visibly extend for as long as you hold it, rather than only appearing once you release it; the status bar should show a running "Recording — N notes captured" count that increases the instant each note starts. Clicking Stop Recording (or Stop) ends the take — whatever note you're still holding at that instant stops growing right there, and the button label reverts to "Record". *(manual: needs a real mic and harmonica; the onset/release diffing, live-growth math, and MIDI-pitch-to-hole resolution are unit-tested — `song_editor::record::tests::*`)*
- [ ] **A bent note is recorded as a bend, not snapped to the nearest natural note**: bend a note down and hold it steady — it should land on the grid with the bend indicator lit at roughly the right depth, the same as a note placed manually via the Bend mod-panel button. *(manual: needs a real mic; `map_pitch`'s bend-resolution is unit-tested via `song_editor::record::tests::spawn_open_note_resolves_pitch_via_map_pitch_including_bends` and `song_editor::midi_import::tests::*`)*
- [ ] **Recording appends rather than replaces**: record a take onto a chart that already has notes elsewhere, then check nothing you had before was deleted or moved — only new notes should appear. *(manual)*
- [ ] **Switching to Edit mode, or clicking Stop, always ends an in-progress recording** — the Record button doesn't keep listening in the background after you've navigated away from it. *(manual)*
- [ ] **Starting Play or Practice while a recording is in progress cleanly ends the recording first** — the currently-held note should stop growing and finalize rather than freezing at length 1 or corrupting later note timings. *(manual)*

## Acceptance criteria → coverage

- Main gameplay chain runs without errors in the observed modes — *manual run + automated state/teardown tests.*
- Play 2D shows HUD and active notes — *manual (rendering).*
- Play 3D initializes the scene and restores state when leaving — *camera restore + teardown automated; scene init manual.*
- Jam Session functional without a traditional chart — *manual.*
- Esc pauses and resumes correctly — *automated (flag + overlay); audio manual.*

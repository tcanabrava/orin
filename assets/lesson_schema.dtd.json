{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "harmonicon/lesson",
  "title": "Harmonicon Lesson Manifest",
  "description": "One lesson in the curriculum: localized title/body keys, an optional playable chart, prerequisites, and pass criteria.",
  "type": "object",
  "required": ["id", "unit", "title_key", "body_key"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier, unique across all lessons. Used as the profile key and in other lessons' prerequisites — never rename one that has shipped."
    },
    "unit": {
      "type": "string",
      "minLength": 1,
      "description": "Unit identifier grouping lessons in the menu. Localized via the Fluent key `lesson-unit-<unit>`."
    },
    "title_key": {
      "type": "string",
      "minLength": 1,
      "description": "Fluent key for the lesson's title."
    },
    "body_key": {
      "type": "string",
      "minLength": 1,
      "description": "Fluent key for the lesson's instructional body text."
    },
    "chart": {
      "type": "string",
      "minLength": 1,
      "description": "Path to a .harpchart, relative to the lesson's own directory. Absent = instructional-only lesson (no playable drill)."
    },
    "prerequisites": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true,
      "description": "Lesson ids that must be passed before this one unlocks."
    },
    "pass_criteria": {
      "type": "object",
      "required": ["type"],
      "oneOf": [
        {
          "required": ["type", "threshold"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "accuracy" },
            "threshold": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1,
              "description": "Minimum overall weighted accuracy (0..1) to pass."
            }
          }
        },
        {
          "required": ["type", "technique", "threshold"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "technique" },
            "technique": {
              "type": "string",
              "enum": ["normal", "bend", "vibrato", "wah-wah", "overblow", "overdraw", "slide", "clean-attack"],
              "description": "Technique bucket name — the same vocabulary SongStats/profile use."
            },
            "threshold": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1,
              "description": "Minimum accuracy (0..1) on that technique's notes to pass."
            }
          }
        },
        {
          "required": ["type", "threshold"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "scale-adherence" },
            "threshold": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1,
              "description": "Minimum fraction (0..1) of notes played during the jam that were at least in-scale. Routes the lesson into Jam Session instead of the normal chart pipeline — see PassCriteria::ScaleAdherence."
            }
          }
        },
        {
          "required": ["type", "threshold"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "chord-tone-adherence" },
            "threshold": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1,
              "description": "Minimum fraction (0..1) of notes played during the jam that were specifically chord tones (stricter than scale-adherence). Routes into Jam Session like scale-adherence — see PassCriteria::ChordToneAdherence."
            }
          }
        },
        {
          "required": ["type", "threshold"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "phrase-discipline" },
            "threshold": {
              "type": "number",
              "exclusiveMinimum": 0,
              "maximum": 1,
              "description": "Minimum fraction (0..1) of notes played outside a rest window of the jam's play/rest bar pattern. Routes into Jam Session like scale-adherence — see PassCriteria::PhraseDiscipline."
            }
          }
        }
      ],
      "description": "How a chart-backed lesson is judged at the results screen (or, for a scale-adherence/chord-tone-adherence/phrase-discipline lesson, by its own \"Finish Lesson\" jam pause-menu button — see PassCriteria::ScaleAdherence). Absent = playing it through at all counts as done (or, for instructional-only lessons, the reader's Mark-as-Done button)."
    },
    "progression": {
      "type": "string",
      "enum": ["standard", "quick-change", "minor", "jazz-blues"],
      "description": "A jam-based lesson's backing progression, seeded into the jam when Start routes into it. Absent = Standard. See menu::lessons::parse_progression."
    },
    "scale": {
      "type": "string",
      "enum": ["first-position", "second-position", "third-position", "major", "minor-pentatonic", "country"],
      "description": "A jam-based lesson's scale for live scale-adherence feedback, seeded into JamScale when Start routes into it. Absent = first-position (the blues hexatonic). See menu::lessons::parse_scale."
    },
    "diagram": {
      "type": "string",
      "enum": ["circle-of-fifths"],
      "description": "An instructional lesson's embedded reference diagram, rendered by the lesson reader alongside the body text. Absent = no diagram. See dialogs::circle_of_fifths."
    },
    "position_cycle": {
      "type": "boolean",
      "description": "A jam-based lesson that periodically calls a new position (cycling JamScale through First/Second/Third position every few bars), seeded into JamPositionCycle when Start routes into it. Absent/false = the jam's scale field stays fixed for the whole session. See jam::position_guide."
    }
  }
}
